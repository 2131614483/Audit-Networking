# 出厂默认配置（请勿直接改，用 custom.yaml 覆盖）
module:
  id: IA-01
  name: 动态风险地图与智能审计计划
  lsb:
    base_url: http://localhost:8080   # LLM 服务总线
  llm:
    prompt_template: templates/prompt.txt
    temperature: 0.2
