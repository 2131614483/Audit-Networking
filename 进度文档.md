# 智能审计编排系统 - 项目进度文档

> 最后更新: 2026-08-08 (端到端审计报告+Web可视化完成，新增待办需求)
> 项目路径: c:\Pythonproject\解决方案详细报告

## 一、项目概述

智能审计编排系统：用户输入审计需求 → DeepSeek LLM 解析需求并匹配模块 → 自动补全上游依赖 → 校验接口契约 → 拓扑排序执行 → 生成审计报告。

### 核心架构

```
用户输入审计需求
    ↓
LLMPlanner (DeepSeek-v4-flash)  ← 需求解析 + 模块选择
    ↓
ContractValidator               ← 接口契约校验 (network_schema.json)
    ↓
DataAligner                     ← 数据对齐层 (实体/时间/模态)
    ↓
TopoExecutor                    ← 拓扑排序 + 逐模块执行 Pipeline.run()
    ↓
ReportGenerator                 ← 审计报告生成
```

### 关键文件索引

| 模块 | 文件 | 说明 |
|------|------|------|
| 编排引擎 | [modules/shared/orchestrator.py](file:///c:\Pythonproject\解决方案详细报告\modules\shared\orchestrator.py) | LLMPlanner + ContractValidator + TopoExecutor + ReportGenerator |
| 接口契约 | [modules/shared/contract.py](file:///c:\Pythonproject\解决方案详细报告\modules\shared\contract.py) | v2.0 强类型契约: TypedField(dtype/unit/nullable/modality) |
| 数据对齐 | [modules/shared/aligner.py](file:///c:\Pythonproject\解决方案详细报告\modules\shared\aligner.py) | EntityAligner + TimeAligner + ModalAligner |
| 网络拓扑 | [modules/shared/network_schema.json](file:///c:\Pythonproject\解决方案详细报告\modules\shared\network_schema.json) | 78模块接口定义 + 45条数据依赖边 |
| 引擎基类 | [modules/shared/base_engine.py](file:///c:\Pythonproject\解决方案详细报告\modules\shared\base_engine.py) | AbstractEngine 模板方法模式 |
| FastAPI后端 | [backend.py](file:///c:\Pythonproject\解决方案详细报告\backend.py) | /api/plan, /api/execute, /api/cases |
| Web界面 | [智能审计编排.html](file:///c:\Pythonproject\解决方案详细报告\智能审计编排.html) | DAG可视化 + 模拟执行 |
| 数据适配层 | [demo/data_adapter.py](file:///c:\Pythonproject\解决方案详细报告\demo\data_adapter.py) | 数据→模块接口对齐规则 |
| 全模块测试 | [demo/test_all_modules.py](file:///c:\Pythonproject\解决方案详细报告\demo\test_all_modules.py) | 78模块逐个跑Pipeline.run() |
| 数据生成器 | [demo/generate_synthetic_data.py](file:///c:\Pythonproject\解决方案详细报告\demo\generate_synthetic_data.py) | 1.072GB多模态模拟数据 |
| 数据说明 | [demo/数据说明.md](file:///c:\Pythonproject\解决方案详细报告\demo\数据说明.md) | 数据合规来源+类型+匹配关系 |

## 二、已完成的工作

### 1. 模块开发 (78个审计模块)
- 12个业务域: FA财务/CO合规/IP-IPO/CM持续/FO舞弊/IT-IT/TA税务/SC供应链/ES-ESG/FI金融/IA内部/CB跨境
- 每模块含: `engine.py`(核心引擎) + `pipeline.py`(管道) + `module.yaml`(元数据) + `config/`(配置) + `tests/`(测试)
- 单元测试: 3045项全绿 (见 modules/*/tests/)

### 2. 智能编排引擎
- **LLMPlanner**: 调用 DeepSeek-v4-flash，SYSTEM_PROMPT 含78模块清单，LLM选模块+确定性算法补全上游依赖
- **ContractValidator**: 按network_schema.json校验接口契约一致性，支持v2.0强类型校验
- **TopoExecutor**: 拓扑排序后逐模块执行，集成DataAligner数据对齐层
- **ReportGenerator**: 汇总各模块输出生成审计报告

### 3. 多模态数据架构改进 (P0+P1完成)
- **强类型契约 v2.0**: TypedField(dtype/unit/nullable/modality/sample_rate) + validate_edge_compatibility
- **数据对齐层**: EntityAligner(USCC硬匹配+名称相似度+并查集闭包) + TimeAligner(7种格式+时区归一) + ModalAligner
- **多模态数据湖**: fa_03 ODS/DWD schema 扩展6个多模态字段(text_content/media_uri/media_modality/event_time/entity_uscc/data_source)

### 4. 数据集 (1.072GB)
- 78模块全覆盖，6种模态(tabular/text/image_ref/video_ref/timeseries/graph)
- 路径: `demo/synthetic_data/<slug>/`
- 全部为程序化生成的模拟数据，合规可用

### 5. 全模块数据集测试
- **78/78 通过 (100%)** — 总耗时 2.9s
- 数据对齐问题: 0 (全部修复)
- 引擎代码bug: 0 (全部修复，见下文)

## 三、已修复的引擎代码bug（共7+5=12项）

所有模块内部代码问题已全部修复，78模块全绿通过：

| 模块 | 错误类型 | 根因 | 修复方式 |
|------|---------|------|---------|
| fi_04 | ImportError | pipeline.py导入RPAEngine，engine.py导出LLMEngine | 统一为LLMEngine |
| ia_03 | ImportError | pipeline.py导入MLEngine，engine.py导出ResourceEngine | 统一为ResourceEngine |
| ia_04 | ImportError | pipeline.py导入MLEngine，engine.py导出DashboardEngine | 统一为DashboardEngine |
| ia_01 | TypeError | self.model为None，_preprocess未调用_load_model | 添加_ensure_loaded惰性初始化 |
| cm_03 | TypeError | 同上 | 同上 |
| cm_04 | TypeError | 同上 | 同上 |
| fo_04 | NameError | data_adapter lambda中`i`未定义（缺少enumerate） | 添加enumerate |
| ia_01 | OperationalError | SQLite ON CONFLICT需要PRIMARY KEY/UNIQUE约束 | portable_db.upsert改用DELETE+INSERT |
| ia_03 | OperationalError | 同上 | 同上 |
| ia_04 | TypeError | sum()初始值0(int)与timedelta相加 | 改用sum(spans, timedelta()) |
| it_05 | SyntaxError | 嵌套f-string引号冲突 `block['hash']` | 提取block_hash变量 |
| es_06 | SyntaxError | 嵌套f-string引号冲突 `tpl['subject']` | 提取tpl_subject变量 |
| ia_03 | 无限循环 | PMX交叉while循环无法终止 | 重写PMX+添加guard上限+降低GA参数 |

## 四、DeepSeek LLM 配置

```
模型: deepseek-v4-flash
BASE URL: https://api.deepseek.com
API KEY: sk-<请在环境变量 DEEPSEEK_API_KEY 中配置>
上下文: 1M tokens, 输出: 384K tokens
价格: 输入1元/百万tokens(缓存未命中), 输出2元/百万tokens
```

配置位置: [orchestrator.py:24-26](file:///c:\Pythonproject\解决方案详细报告\modules\shared\orchestrator.py#L24-L26)

## 五、数据→模块接口对齐规则

`demo/data_adapter.py` 实现了78个模块的数据适配：
- 读取 synthetic_data 的数据文件 (records.jsonl/text_corpus.txt/graph.json等)
- 按 engine._preprocess 期望的 key 构造 dict
- 按模块 action 路由提供对应参数

### 模块输入格式分类

| 类型 | 数量 | 特点 | 代表模块 |
|------|------|------|---------|
| 严格校验型 | 10 | raise ValueError，要求dict+特定key | fa_02, co_01, ip_03, cm_01, ta_01, sc_01 |
| 宽松容错型 | 20 | 支持字符串/空值兜底 | fa_08, ip_02, ta_02-ta_06, cb_04/05/06 |
| action路由型 | 6 | 按action字段分发 | co_03, cm_03, ia_01, ia_02, cb_05, cb_06 |
| TODO stub透传 | 44 | _collect仅return input_data | 多数CM/IA/CB/IT模块 |

## 六、端到端审计报告 + Web可视化（已完成）

### 已完成的新功能

| 功能 | 文件 | 说明 |
|------|------|------|
| 端到端审计脚本 | [demo/run_audit_report.py](file:///c:\Pythonproject\解决方案详细报告\demo\run_audit_report.py) | LLM规划→拓扑执行→生成audit_data.json + audit_report.json |
| MD报告生成器 | [demo/generate_md_report.py](file:///c:\Pythonproject\解决方案详细报告\demo\generate_md_report.py) | JSON→Markdown报告（含Mermaid拓扑图） |
| Web可视化界面 | [web/index.html](file:///c:\Pythonproject\解决方案详细报告\web\index.html) + [web/style.css](file:///c:\Pythonproject\解决方案详细报告\web\style.css) + [web/galaxy.js](file:///c:\Pythonproject\解决方案详细报告\web\galaxy.js) | 星云节点风格Canvas可视化，白底黑字 |
| 启动器 | [launch.py](file:///c:\Pythonproject\解决方案详细报告\launch.py) | 一键启动本地服务器+自动打开浏览器 |

### Web可视化特性
- **星云节点风格**：模块节点正弦漂浮+家族聚拢，动态星云感
- **距离自动连线**：近距节点自动绘制淡色连线
- **DAG数据流边**：贝塞尔曲线+箭头，选中时高亮上下游
- **滚轮缩放+拖拽平移**：以鼠标为中心缩放
- **审计发现三级折叠**：来源模块→严重等级→具体发现（可展开详情）
- 金额自动提取转万/亿单位

### 当前审计报告数据（IPO财务规范性诊断场景）
- 模块数: 10/10 成功
- 审计发现: 58条（来自4个模块）
  - fa_12 关联交易披露完整性检查: 50条（高50）
  - ip_04 AI财务规范性智能诊断: 5条（高2中3）
  - ip_03 知识图谱历史沿革梳理: 2条（高2）
  - ip_01 IPO审计智能加速平台: 1条（中1）

### 审计发现提取（_extract_findings）
配置位置: [orchestrator.py:1460](file:///c:\Pythonproject\解决方案详细报告\modules\shared\orchestrator.py#L1460)
- 硬编码支持: fa_10, fa_11, fa_12, co_04, co_05, ip_01, ip_03, ip_04
- 通用兜底: 扫描 anomalies/violations/warnings/risks 字段
- severity 映射: high→高, medium→中, low→低

### orchestrator._collect_inputs 改动
[orchestrator.py:1058](file:///c:\Pythonproject\解决方案详细报告\modules\shared\orchestrator.py#L1058)
- 新增模块专属输入支持：`user_input[slug]` 适配数据优先注入
- 解决了编排器数据路由与引擎期望格式不匹配的问题

## 七、待办需求：数据溯源 + AI总结（尚未实现）

> **此需求尚未实现，为下一步开发目标。**

### 需求描述
用户需要审计发现能够**逐层下钻到具体数据集的原始数据条目**，并在底层由 **DeepSeek AI 生成总结分析**。

### 目标层级结构（5层下钻）

```
第1层: 来源模块（如 fa_12 关联交易披露完整性检查）
  └─ 第2层: 严重等级（高/中/低）
       └─ 第3层: 具体发现条目（如"关联交易未披露 TX-000001"）
            └─ 第4层: 关联的原始数据集（如 synthetic_data/fa_12/transactions.jsonl）
                 └─ 第5层: 原始数据记录（jsonl 中的具体行，含完整字段）
                      → AI总结: DeepSeek 对该条数据的分析结论
```

### 需要改造的文件

| 文件 | 改造内容 | 状态 |
|------|---------|------|
| [demo/data_adapter.py](file:///c:\Pythonproject\解决方案详细报告\demo\data_adapter.py) | `load_raw_data` 返回时给每条记录打上 `_source_file` + `_row_index` 标记；适配函数透传这些标记 | **待实现** |
| [modules/shared/orchestrator.py](file:///c:\Pythonproject\解决方案详细报告\modules\shared\orchestrator.py) | `_extract_findings` 提取发现时，通过 tx_id/finding_id 等关键字反查原始数据记录，写入 `source_data` 字段 | **待实现** |
| [web/galaxy.js](file:///c:\Pythonproject\解决方案详细报告\web\galaxy.js) | `renderFindings` 改为5层折叠：模块→严重度→发现→数据集→原始记录+AI总结 | **待实现** |
| [web/style.css](file:///c:\Pythonproject\解决方案详细报告\web\style.css) | 新增第4/5层折叠样式 | **待实现** |
| [launch.py](file:///c:\Pythonproject\解决方案详细报告\launch.py) | 新增 `/api/ai_summary` 接口，调用 DeepSeek 生成数据条目分析 | **待实现** |
| 新增: modules/shared/deepseek_client.py | DeepSeek API 封装（复用 orchestrator 的配置） | **待实现** |

### 数据溯源实现方案

**关键问题**：发现条目（如 fa_12 的 missing_items）如何关联到原始数据记录？

**方案**：
1. `data_adapter.py` 的 `_load_jsonl` 在加载时给每条记录注入元数据：
   ```python
   rec["_source_file"] = "transactions.jsonl"  # 原始文件名
   rec["_row_index"] = i  # 行号(0-based)
   rec["_dataset"] = slug  # 数据集标识
   ```
2. 适配函数在构造模块输入时保留这些标记（不剥离）
3. 引擎处理时透传到输出（如 fa_12 的 missing_items 保留 tx_id）
4. `_extract_findings` 通过 tx_id 在 `load_raw_data(slug)` 的结果中查找匹配记录
5. 将匹配到的原始记录（含 `_source_file`/`_row_index`）写入发现的 `source_records` 字段

**字段映射表**（发现条目 → 原始数据的关键字段）：

| 模块 | 发现中的关键字段 | 原始数据集 | 匹配字段 |
|------|----------------|-----------|---------|
| fa_12 | tx_id (TX-000001) | transactions.jsonl | tx_id |
| fa_11 | tx_id | transactions.jsonl | tx_id |
| fa_10 | network节点名 | shareholders.jsonl | name |
| ip_01 | finding_id | records.jsonl | id |
| ip_03 | event_seq | history_events.jsonl | 行号 |
| ip_04 | problem_id | records.jsonl | id |
| co_04 | tx_id/sar_id | transactions.jsonl | tx_id |

### AI总结实现方案

**接口设计**：
```
POST /api/ai_summary
Body: { "slug": "fa_12", "finding": {...}, "source_records": [{...}] }
Response: { "summary": "DeepSeek生成的分析文本" }
```

**DeepSeek 调用**（复用 orchestrator.py 的配置）：
- 模型: deepseek-v4-flash
- API Key: sk-<请在环境变量 DEEPSEEK_API_KEY 中配置>
- BASE URL: https://api.deepseek.com
- Prompt 模板:
  ```
  你是审计专家。请分析以下审计发现和对应的原始数据记录，给出专业结论。
  审计发现：{title} - {detail}
  原始数据：{source_records的JSON}
  请从以下角度分析：
  1. 数据异常点
  2. 潜在风险
  3. 建议措施
  ```

**前端交互**：
- 第5层（原始数据记录）展开时，自动调用 `/api/ai_summary` 获取AI总结
- AI总结显示在数据记录下方，带 loading 动画
- 总结结果缓存到前端，避免重复调用

### 注意事项
- DeepSeek API 调用有延迟（1-3秒），需异步加载
- 原始数据记录可能含敏感字段，展示时注意脱敏
- 数据溯源依赖 tx_id 等关键字段，需确保适配层不丢失这些字段
- `_load_jsonl` 注入的 `_source_file`/`_row_index` 是内部字段，引擎处理时不能报错（需确保引擎容错）

## 八、运行方式

### 启动后端服务
```bash
.venv\Scripts\python.exe backend.py
# FastAPI on http://localhost:9000
```

### 启动Web界面
```bash
.venv\Scripts\python.exe -m http.server 8765
# 访问 http://localhost:8765/智能审计编排.html
```

### 运行全模块测试
```bash
.venv\Scripts\python.exe demo\test_all_modules.py
.venv\Scripts\python.exe demo\analyze_report.py  # 分析报告
```

### 运行端到端LLM流程
```bash
.venv\Scripts\python.exe -c "
from modules.shared.orchestrator import run_audit_with_llm
result = run_audit_with_llm('审查关联交易定价公允性')
print(result.final_report)
"
```

## 九、关键技术约束

1. **模块代码已全部修复**：78/78模块测试全绿通过，无待修复bug
2. 知识图谱可视化必须离线工作(无CDN)
3. file://协议下新开tab用button+window.open()
4. 统一配色: 背景#ffffff, 主文本#1a1d29, 面板#f8f9fa
5. 数据格式标准色: json/dict(蓝), parquet(绿), stream(橙), graph(紫), jsonl(青)
6. **数据溯源(待实现)**: _load_jsonl注入的_source_file/_row_index/_dataset是内部字段，引擎需容错
7. **AI总结(待实现)**: DeepSeek调用需异步，前端用loading动画；结果前端缓存避免重复调用
8. **DeepSeek配置**: 复用 orchestrator.py 的 LLM 配置（模型/API Key/BASE URL）
