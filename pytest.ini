[pytest]
# 各模块 tests 目录存在同名文件（test_api/test_engine/test_pipeline），
# 默认 prepend 模式会产生基名冲突（import file mismatch）。
# importlib 模式按文件路径独立导入，无需为每个 tests 目录补 __init__.py。
testpaths = modules
addopts = --import-mode=importlib -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
