# 项目反思与经验提示词

> 基于「智能审计可视化平台」开发过程中遇到的真实问题，提炼为可复用的反思提示词（Checklist）与经验总结。
> 适用于：同类 Web 可视化项目、Python 桌面启动器、Tkinter GUI、Windows 批处理脚本的开发与排错。

---

## 一、反思提示词（Checklist）

> 以下提示词可粘贴给 AI 作为开发/排错的上下文约束，也可作为人工自检清单。

### 1. 浏览器兼容性自检

```
在交付前端页面前，请逐项确认：
- [ ] 是否添加 <meta name="renderer" content="webkit"> 强制双核浏览器（360/搜狗/猎豹）使用 Chromium 内核？
- [ ] 是否添加 <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">？
- [ ] 是否添加 <meta name="force-rendering" content="webkit">（360 特定）？
- [ ] CSS 是否对关键属性同时提供 var() 变量与硬编码值双重写法（老内核不支持 CSS 变量）？
- [ ] JS 入口是否检测关键 ES6+ 特性（Promise/fetch/Symbol），不支持时给出友好提示而非白屏？
- [ ] 是否用 try/catch 包裹 init()，失败时 showCompatError() 显示可读错误而非静默崩溃？
- [ ] 是否在真实双核浏览器（非仅 Chrome）中验证过渲染？
```

### 2. Canvas 可视化初始化自检

```
Canvas 2D 可视化项目，请确认：
- [ ] resizeCanvas() 是否处理了 wrapper.clientWidth=0 的情况（DOM 未完成布局时）？
- [ ] 是否对 canvas 尺寸设了最小兜底值（如 < 50 时用 window 尺寸）？
- [ ] 节点布局逻辑是否提取为可重复调用的函数（如 layoutNodes(w,h)），而非只在 init 调用一次？
- [ ] resize 事件触发时，是否重新布局节点（检测尺寸显著变化后重排）？
- [ ] init() 是否在 DOMContentLoaded 或之后调用，避免布局未完成？
- [ ] 是否验证过节点的 baseX/baseY 不全为 0（否则全堆在原点 = 空白）？
- [ ] 是否用 getImageData 检查过 canvas 非空像素比例（>1% 才算正常渲染）？
```

### 3. Python 子进程管理自检

```
用 subprocess.Popen 启动子进程（如 Web 服务器）时，请确认：
- [ ] 是否设置 PYTHONUNBUFFERED=1 和 PYTHONIOENCODING=utf-8，保证输出实时可读？
- [ ] 读取子进程 stdout 的线程，是否只在 proc.poll() 确认退出后才发"进程退出"消息？
- [ ] 是否避免用 proc.wait(timeout=N) 的超时作为"进程退出"判断（超时会抛异常，不代表退出）？
- [ ] for line in proc.stdout 循环结束后，是否用 proc.poll() 二次确认而非直接判定退出？
- [ ] stop() 是否先 terminate() 再 wait(timeout)，超时才 kill()？
- [ ] Windows 下是否加了 creationflags=CREATE_NO_WINDOW 避免弹出黑窗？
```

### 4. Tkinter GUI 异常处理自检

```
Tkinter GUI 项目，请确认：
- [ ] sys.excepthook 是否已安装，未捕获异常写入 crash.log 而非静默丢失？
- [ ] 自定义 Tk 异常处理函数是否避免了递归调用（如不能再调 report_callback_exception）？
- [ ] 主题/字体配置是否用 try/except 逐个尝试，不支持就跳过（而非整体崩溃）？
- [ ] 是否在 main() 入口设置 DPI awareness，但放在 try/except 中？
- [ ] 后台线程是否通过 queue + after() 轮询更新 UI，而非直接跨线程操作控件？
```

### 5. Windows 批处理脚本自检

```
编写 .bat 启动脚本时，请确认：
- [ ] 文件是否为无 BOM 的 ANSI/UTF-8 编码？
- [ ] 是否避免在 echo 中使用 Unicode 边框字符（═│┌┐）和中文标点（可能导致 cmd 解析错误）？
- [ ] 是否用 echo( 前缀而非 echo. （echo. 在某些情况会报错）？
- [ ] 每个错误分支是否都 goto :end，且 :end 处必有 pause（防止窗口闪退）？
- [ ] 是否用 setlocal EnableExtensions DisableDelayedExpansion（避免变量展开问题）？
- [ ] chcp 65001 是否加了 >nul 2>&1 静默（失败不影响流程）？
- [ ] 是否用 cd /d "%~dp0" 切到脚本所在目录？
- [ ] 关键步骤是否写日志（>> "%BAT_LOG%" echo ...）便于排错？
```

### 6. 环境与依赖管理自检

```
项目启动器，请确认：
- [ ] 是否自动检测/创建虚拟环境（venv），而非要求用户手动配置？
- [ ] 是否实现了依赖检测（_check_deps_installed），缺失时自动 pip install？
- [ ] 依赖检测的 subprocess timeout 是否留足余量（openai 等大包冷 import 可能 5-10s）？
- [ ] 是否区分"venv 不存在"和"venv 存在但依赖缺失"两种情况？
- [ ] pip install 超时是否设为 300s（5 分钟，首次安装大包可能慢）？
- [ ] 找不到 Python 时是否给出友好提示（引导到 python.org）？
```

### 7. Web 服务器与端口管理自检

```
本地 Web 服务器项目，请确认：
- [ ] 启动前是否检测端口占用，被占用时递增找空闲端口？
- [ ] 是否明确文档化"正确启动方式"，避免用户误用 python -m http.server（无 API 路由）？
- [ ] SimpleHTTPRequestHandler 子类是否实现了 do_GET/do_POST 的 API 路由？
- [ ] 静态数据文件是否同步到 web 目录作为兜底（前端 fetch 二级回退）？
- [ ] 是否在启动器中提供"停止"功能，避免旧进程残留占用端口？
```

### 8. 前端数据加载容错自检

```
前端 fetch 数据，请确认：
- [ ] 是否实现二级兜底：API 接口失败 → 静态 JSON 文件 → 友好错误提示？
- [ ] fetch 失败时是否显示具体错误（HTTP 状态/异常 message），而非空白？
- [ ] 是否避免用 alert/confirm 弹窗（被浏览器拦截），改用 DOM 内提示？
- [ ] 错误提示中是否给出可操作的修复命令（如"请用 python launch.py 启动"）？
```

### 9. 测试套件自检

```
Python GUI/子进程相关测试，请确认：
- [ ] 测试模块顶部是否设置 LANG=C / LC_ALL=C（隔离 Tcl/Tk locale 问题）？
- [ ] subprocess 调用的 timeout 是否比实际耗时多 3-5 倍余量？
- [ ] 测试间是否避免共享 Tk root 实例（每个测试独立创建+销毁）？
- [ ] 真实子进程测试是否验证"N 秒存活 + 干净退出码"（而非仅检查启动成功）？
- [ ] 依赖检测测试是否在冷/热缓存两种情况下都验证过？
```

---

## 二、经验总结（Lessons Learned）

### 经验 1：Canvas 初始化的时序陷阱

**问题**：`init()` 在 DOM 布局完成前调用 `resizeCanvas()`，此时 `wrapper.clientWidth=0`，导致 canvas 被设成 0×0，节点布局算出 `cx=0, cy=0`，所有节点堆在原点 → 画布空白。

**教训**：
- Canvas 项目的 `clientWidth` 在 DOM 未布局时是 0，**绝不能直接用**，必须兜底。
- 节点布局逻辑必须**可重复调用**（提取为 `layoutNodes(w, h)`），让 resize 能触发重排。
- 排错时用 `getImageData` 检查非空像素比例，能快速判断"画了但看不见" vs "完全没画"。

**通用原则**：任何依赖 DOM 尺寸的计算，都要处理尺寸为 0 的边界情况，且在尺寸变化时能重新计算。

---

### 经验 2：`python -m http.server` 不能替代自定义服务器

**问题**：用户误用 `python -m http.server 8765` 启动，该命令只提供静态文件服务，**不执行 launch.py 的 API 路由**（`/api/audit_data`），前端 fetch 必然失败 → 空白页。

**教训**：
- `SimpleHTTPRequestHandler` 子类化后才有 API 能力，裸 `http.server` 没有。
- 启动方式必须在文档和启动器中**强约束**，不能假设用户会用对命令。
- 前端 fetch 失败时，错误提示应明确告知"请用 `python launch.py` 启动"，降低排错成本。

**通用原则**：提供唯一正确的入口（启动器），避免多种启动方式并存导致用户混淆。

---

### 经验 3：端口残留进程是"幽灵 Bug"的常见来源

**问题**：旧进程占用 8765 端口（跑修复前代码），新启动器被迫改用 8766，浏览器打开旧标签页看到空白，误以为修复无效。

**教训**：
- 排错时第一步应检查端口占用：`Get-NetTCPConnection -LocalPort <port>`。
- 用 `Get-CimInstance Win32_Process` 查 CommandLine，区分正确/错误的启动进程。
- 启动器应提供"强制停止"能力，清理残留进程。

**通用原则**：遇到"修复了但没生效"，先怀疑是旧进程/缓存，而非代码问题。

---

### 经验 4：批处理脚本的 Unicode 陷阱

**问题**：`.bat` 文件中使用 `═│┌┐` 等 Unicode 边框字符和中文，在 cmd.exe 中导致命令解析错误 → 闪退。

**教训**：
- Windows cmd.exe 的编码处理脆弱，批处理脚本**只用纯 ASCII** 最安全。
- 用 `echo(` 前缀而非 `echo.`，前者更兼容。
- 每个分支都 `goto :end`，`:end` 处必有 `pause`，杜绝闪退。
- 编码用 `chcp 65001 >nul 2>&1` 静默设置，失败不阻断流程。

**通用原则**：Windows 批处理脚本的容错优先级高于美观，能用 ASCII 就别用 Unicode。

---

### 经验 5：Tkinter 异常处理不能递归

**问题**：自定义 `on_tk_error` 函数中调用了 `dr.report_callback_exception`，而后者又会触发 `on_tk_error` → 无限递归 → 栈溢出 → GUI 闪退。

**教训**：
- 异常处理函数**绝对不能**调用会再次触发自身的机制。
- `sys.excepthook` 安装后，只写日志 + 弹框，不再回调 Tk 的内部异常处理。
- 主题/字体配置用 `try/except` 逐个尝试，任何一项失败都不应崩溃。

**通用原则**：异常处理代码本身必须是最简、最安全的，不能引入新的异常路径。

---

### 经验 6：子进程退出判断要用 `poll()` 而非 `wait(timeout)`

**问题**：`_read_stdout` 用 `proc.wait(timeout=5)`，超时抛异常被误判为"进程退出"，错误触发 `stop()`，导致服务器被杀。

**教训**：
- `for line in proc.stdout` 循环结束 = stdout 关闭 = 进程**可能**退出，但需二次确认。
- 用 `proc.poll() is not None` 确认退出状态，`poll()` 立即返回不阻塞。
- `wait(timeout=N)` 的超时**不代表退出**，只是"还没退出"，不能作为判断依据。

**通用原则**：区分"阻塞超时"和"真实退出"，用非阻塞的 `poll()` 做状态判断。

---

### 经验 7：`PYTHONUNBUFFERED=1` 是子进程日志实时性的关键

**问题**：launch.py 的 print 输出被缓冲，GUI 日志窗口长时间空白，误以为卡死。

**教训**：
- 子进程必须设 `PYTHONUNBUFFERED=1`，否则 print 输出要等缓冲区满或进程结束才 flush。
- 同时设 `PYTHONIOENCODING=utf-8`，避免 Windows 下中文输出乱码。
- 这两个环境变量是 Python 子进程管理的"标配"。

**通用原则**：任何需要实时读取子进程输出的场景，都要关闭输出缓冲。

---

### 经验 8：subprocess timeout 要留足余量

**问题**：`_check_deps_installed` 用 `timeout=10`，但 openai 等大包冷 import 实测 4.8s，在系统繁忙时超时 → 误判依赖未装 → 触发无谓的重新 pip install。

**教训**：
- subprocess timeout 要按"最坏情况"设置，冷启动/系统繁忙时耗时翻倍。
- 依赖检测这种"只读"操作，timeout 留 30s 比较安全。
- `except Exception: return False` 会吞掉超时异常，导致误判——要在注释中说明判断逻辑。

**通用原则**：timeout 不是越短越好，要考虑冷启动、缓存失效、系统负载等最坏情况。

---

### 经验 9：前端数据加载必须二级兜底

**问题**：前端只 fetch API，API 失败就空白；后端数据文件未同步到 web 目录，兜底也失败。

**教训**：
- 数据加载链路：`fetch("/api/...")` → 失败 → `fetch("static.json")` → 失败 → 友好提示。
- 后端启动时把数据文件**同步一份**到 web 静态目录，作为兜底数据源。
- 最终兜底提示要给出**可操作的修复命令**，而非"加载失败"四个字。

**通用原则**：前端永远不能假设 API 一定可用，要有静态兜底 + 可操作的错误提示。

---

### 经验 10：测试环境的 locale 隔离

**问题**：系统 Python 312 的 `tk8.6/msgs/zh_cn.msg` 缺失，连续创建/销毁 Tk 实例后，msgcat 重新加载 locale 抛 `TclError` → 测试失败。

**教训**：
- Tkinter 测试在模块顶部设 `LANG=C` / `LC_ALL=C`，避免加载中文 locale 文件。
- 测试间避免共享 Tk root，每个测试独立 `Tk()` + `destroy()`。
- 环境问题导致的测试失败，不影响真实运行（用真实子进程测试验证，如 test_t10）。

**通用原则**：测试环境要隔离系统级配置（locale/编码/环境变量），避免环境差异导致测试不稳定。

---

## 三、排错方法论

遇到"页面空白/不工作"类问题，按以下顺序排查：

```
1. 数据层：数据文件是否存在？大小是否正常？（Get-Item 查文件大小）
2. 服务层：服务器是否在运行？端口是否被占用？（Get-NetTCPConnection）
         启动方式是否正确？（Get-CimInstance Win32_Process 查 CommandLine）
3. 接口层：API 是否返回正确数据？（浏览器/curl 直接访问 /api/...）
4. 前端层：JS 是否报错？（浏览器控制台 console messages）
         数据是否加载成功？（document.evaluate 查 auditData 状态）
5. 渲染层：Canvas 是否有尺寸？（canvas.width/height）
          节点坐标是否合理？（baseX/baseY 是否全为 0）
          是否真的画了东西？（getImageData 查非空像素比例）
6. 环境层：是否有旧进程残留？（端口占用 + CommandLine 对比）
          是否用了错误的启动命令？
```

**核心原则**：从底层（数据/服务）到上层（渲染），逐层验证，避免在错误层级反复排查。
